# Environment / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Databases
*.db
*.sqlite
*.sqlite3

# uv
.python-version

# Library Skills - regenerated from installed packages via `uvx library-skills`,
# see AGENTS.md. Not committed: content is derived/vendored and goes stale the
# moment fastapi/sqlmodel is upgraded.
.agents/

# Claude Code local tool state (skills installed above, scheduled-task locks, etc.)
.claude/

# Docker bind-mounted app data (SQLite file when running via docker-compose)
# Anchored to repo root only - scripts/data/ holds the committed seed dataset.
/data/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
